# Menu Admin Dashboard - Setup Instructions

## المتطلبات
- Node.js 18+
- npm أو yarn

## التثبيت

### 1. تثبيت المتعلقات
```bash
npm install
```

### 2. متغيرات البيئة
أنشئ ملف `.env.local` في جذر المشروع:

```env
# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:5000
```

### 3. بدء خادم التطوير
```bash
npm run dev
```

التطبيق سيكون متاحاً على: **http://localhost:3000**

## بيانات تسجيل الدخول التجريبية

- **البريد الإلكتروني:** admin@example.com
- **كلمة المرور:** password123

## الميزات الرئيسية

### 1. المصادقة والأمان
- ✅ تسجيل الدخول آمن مع تخزين التوكن في HTTP-only cookies
- ✅ حماية الطرق بـ Middleware
- ✅ معالجة تلقائية للأخطاء 401

### 2. إدارة القائمة
- **الأقسام:** إنشاء وتعديل وحذف أقسام الطعام
- **الأصناف:** إدارة أصناف القائمة مع الأسعار والصور
- **الطاولات:** إعداد وإدارة طاولات المطعم

### 3. التخصيص
- 🌙 **Dark/Light Mode:** تبديل سلس بين الوضع الليلي والنهاري
- 🌐 **اللغات:** دعم كامل للعربية والإنجليزية مع RTL
- ⚙️ **الإعدادات:** تغيير كلمة المرور والمفضلات الشخصية

### 4. التصميم
- 📱 **Responsive Design:** يعمل بشكل مثالي على جميع الأجهزة
- ✨ **Atomic Design Pattern:** بنية منظمة وقابلة لإعادة الاستخدام
- 🎨 **Tailwind CSS:** تصميم حديث وجميل

## هيكل المشروع

```
src/
├── app/                    # Next.js App Router
│   ├── admin/             # صفحات الإدارة المحمية
│   ├── dashboard/         # لوحة التحكم
│   ├── login/             # صفحة تسجيل الدخول
│   ├── settings/          # إعدادات المستخدم
│   └── layout.tsx         # التخطيط الرئيسي
├── api/                   # خدمات API
│   ├── client.ts          # إعدادات Axios
│   └── services/          # خدمات محددة (categories, items, tables)
├── components/            # المكونات
│   ├── atoms/            # مكونات أساسية (Button, Input, Card)
│   ├── molecules/        # مكونات مركبة (Modal, NotificationContainer)
│   └── organisms/        # مكونات معقدة (Sidebar, Header, Forms)
├── hooks/                 # Custom Hooks
├── store/                 # Zustand state management
├── i18n/                  # الترجمات والتكوينات
└── utils/                 # دوال مساعدة
```

## الأوامر المتاحة

### التطوير
```bash
npm run dev
```
بدء خادم التطوير مع hot reload

### البناء
```bash
npm run build
```
إنشاء بناء إنتاجي محسّن

### الإنتاج
```bash
npm start
```
تشغيل خادم الإنتاج

### الفحص
```bash
npm run lint
```
التحقق من جودة الكود

## المتطلبات الأساسية للخادم الخلفي

يجب أن يوفر الخادم الخلفي API endpoints التالية:

### Authentication
- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/auth/logout` - تسجيل الخروج
- `GET /api/auth/me` - الحصول على بيانات المستخدم الحالي

### Categories
- `GET /api/menu/categories` - الحصول على جميع الأقسام
- `GET /api/menu/categories/:id` - الحصول على قسم محدد
- `POST /api/menu/categories` - إنشاء قسم جديد
- `PUT /api/menu/categories/:id` - تحديث قسم
- `DELETE /api/menu/categories/:id` - حذف قسم

### Menu Items
- `GET /api/menu/items` - الحصول على جميع الأصناف
- `GET /api/menu/items/:id` - الحصول على صنف محدد
- `POST /api/menu/items` - إنشاء صنف جديد
- `PUT /api/menu/items/:id` - تحديث صنف
- `DELETE /api/menu/items/:id` - حذف صنف

### Tables
- `GET /api/restaurant/tables` - الحصول على جميع الطاولات
- `GET /api/restaurant/tables/:id` - الحصول على طاولة محددة
- `POST /api/restaurant/tables` - إنشاء طاولة جديدة
- `PUT /api/restaurant/tables/:id` - تحديث طاولة
- `DELETE /api/restaurant/tables/:id` - حذف طاولة

## استكشاف الأخطاء

### الطلبات اللانهائية
إذا واجهت طلبات API متكررة:
1. تحقق من `useAsync` dependencies
2. تأكد من أن الدوال لا تُعاد بناؤها في كل render
3. استخدم `useCallback` للدوال الثابتة

### مشاكل التصميم
- تأكد من أن `tailwind.config.ts` يتضمن جميع الملفات
- تحقق من `postcss.config.js` configuration
- امسح `.next` و`node_modules` وأعد التثبيت إذا لزم الأمر

### مشاكل المصادقة
- تأكد من أن الخادم يرسل `authToken` في response
- تحقق من أن الـ middleware يقرأ الـ cookie بشكل صحيح
- تحقق من `withCredentials: true` في axios client

## الدعم والتطوير

للمساهمة أو الإبلاغ عن الأخطاء:
1. إنشاء issue في GitHub
2. توفير وصف تفصيلي للمشكلة
3. إرفاق رسالة الخطأ والـ screenshot إن أمكن

## الترخيص

هذا المشروع مرخص تحت MIT License

---

**آخر تحديث:** نوفمبر 2024
