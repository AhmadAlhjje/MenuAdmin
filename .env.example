# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:5000

# App Configuration
NODE_ENV=development

# Theme (optional - will use system preference if not set)
# THEME=light

# Language (optional - defaults to en)
# LANGUAGE=en
